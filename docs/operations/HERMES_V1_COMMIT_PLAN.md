# HERMES_V1_COMMIT_PLAN

**Milestone:** EPIC-008.2 Freeze — Phase 5 (grouping ONLY)
**Date:** 2026-07-21
**STATUS: NO COMMITS PERFORMED.** This is a recommended grouping for the human
maintainer to apply. The working tree currently has **155 changed/untracked
paths** spanning multiple EPICs; they MUST NOT be bundled into one commit.

---

## Current Tree State

```
git status --short | sed 's/ .*//' | sort | uniq -c
   20   M    (tracked, modified)
  135   ??   (untracked)
    1   A    (added, staged)
    2   AM   (added then modified)
```

The EPIC-008.1 footprint is 8 files (all untracked `??`). The remaining 147
paths belong to other EPICs (005.x, 006, 006.5, 007, 008) and unrelated
foundation work — they should be committed under their own milestone PRs.

---

## Recommended Commit Groups

### Group A — EPIC-008.1 (Foundation consolidation) — STAGE EXPLICITLY
```
git add \
  hermes/services/activation/providers/deployment/backends/cloudflare-exec.ts \
  hermes/services/activation/providers/deployment/guardrails.ts \
  hermes/services/activation/providers/deployment/workflow.ts \
  hermes/services/activation/providers/bootstrap.ts \
  hermes/services/activation/providers/deployment/stage-deploy.ts \
  hermes/services/activation/providers/deployment/__tests__/epic007.launch.test.ts \
  hermes/tsconfig.epic008.json \
  docs/architecture/EPIC-008.1_BASELINE.md \
  docs/architecture/EPIC-008.1_WORKING_TREE_ASSESSMENT.md \
  docs/architecture/EPIC-008.1_COMPLETION_REPORT.md
```
Message: `EPIC-008.1: consolidate Foundation v1.0 (B1 token, D1 ledger fix, Phase-3 ledger wire)`

### Group B — EPIC-008.2 Freeze docs (this milestone) — STAGE EXPLICITLY
```
git add \
  docs/operations/HERMES_V1_FREEZE_REPORT.md \
  docs/operations/HERMES_V1_RELEASE_READINESS.md \
  docs/operations/HERMES_V1_COMMIT_PLAN.md
```
Message: `EPIC-008.2: Foundation freeze report + release readiness + commit plan`

### Groups C–N — OTHER EPICs (owner-separated, NOT bundled)
The remaining ~145 paths (EPIC-005.x → 008 source, docs, and unrelated
foundation modules under `audit/`, `services/security/`, `admin/console/`,
etc.) should be split per their originating EPIC and reviewed independently.
Do NOT `git add -A`.

---

## Hard Constraints for the Committer

1. **Do not bulk-commit.** The 155-path tree mixes EPIC-008.1 (mine), other
   EPIC foundation work (untracked), and 20 tracked modifications of unrelated
   modules. A single `add -A && commit` would silently bundle unreviewed changes.
2. **Secrets:** none in source (verified). All credentials via `SecretSource`.
3. **Deploy:** not authorized by any EPIC. Live rollout of `CLOUDFLARE_API_TOKEN`
   requires a fresh Workers token + separate deploy approval (R-CF).
4. **Order:** land Group A, then Group B. Other groups per their EPIC owners.

---

*Generated by EPIC-008.2 Phase 5. No `git` mutation was executed.*
