{
  "name": "@workspace/workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash ../scripts/extract-version.sh && wrangler deploy",
    "prebuild": "bash ../scripts/extract-version.sh",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "@cloudflare/workers-types": "^4.20250713.0",
    "@types/node": "^22.20.1",
    "typescript": "~5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.0.0"
  }
}